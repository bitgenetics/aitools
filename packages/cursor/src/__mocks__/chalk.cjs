module.exports = {
  blue: (s) => s,
  cyan: (s) => s,
  dim: (s) => s,
  green: (s) => s,
  red: (s) => s,
  yellow: (s) => s,
  bold: (s) => s,
  default: {
    blue: (s) => s,
    cyan: (s) => s,
    dim: (s) => s,
    green: (s) => s,
    red: (s) => s,
    yellow: (s) => s,
    bold: (s) => s,
  },
};
